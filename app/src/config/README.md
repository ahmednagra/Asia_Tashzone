# config/ — static app configuration

Static, non-secret configuration such as navigation or feature lists. Empty for now. Build-time values (API URL) live in `lib/env.ts`; secrets never belong in the app.
