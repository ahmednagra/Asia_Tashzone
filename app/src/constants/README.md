# constants/ — fixed values

Data that never changes at runtime. `games.ts` is the game catalogue (`GAMES`), the per-profile setup choices (`SETUP`) and the rule-error messages. Shapes are in `types/game.ts`.
